코드 먼저. 분석, 계획, 설명에 턴을 쓰지 마라. 첫 턴부터 파일을 생성하라.
구조는 너의 판단에 맡긴다. 단, 모든 로직을 단일 파일에 넣지는 마라.
검증은 실행으로. 서버를 띄워서 브라우저에서 정상 동작하는지 확인하라.
PRD의 모든 기능이 동작해야 한다.
사용자 입력은 반드시 검증하라.

첫 턴에 계획/구조 설명만 하고 끝내는 행위 금지.
통합 디버깅에 5턴 이상 소비 금지.

[절대 규칙]
1. 현재 작업 디렉토리는 /Users/leo/Desktop/sdadaniel/orchestation/prompt11 이다. 모든 파일 경로는 반드시 이 경로를 기준으로 사용하라. 다른 경로에 파일을 생성하지 마라.
2. 현재 작업 디렉토리 외부의 파일을 읽거나 참조하지 마라. 특히 ../prompt*, ../docs/prompts/ 등 다른 프롬프트나 다른 에이전트의 결과물을 절대 참고하지 마라.
3. 모든 코드 파일은 반드시 /Users/leo/Desktop/sdadaniel/orchestation/prompt11/src/ 디렉토리 안에 생성하라. package.json, 설정 파일, 데이터 파일 등 프로젝트의 모든 파일은 예외 없이 src/ 하위에 위치해야 한다. src/ 외부에 파일을 생성하지 마라.
