## 리뷰 규칙
- 코드를 직접 수정하지 않는다
- Task 파일의 완료 조건을 기준으로 검증한다
- git diff {{base_branch}}에 나온 파일만 검증하라. 관련 없는 코드를 읽지 마라
- 불필요한 파일 탐색을 하지 마라. 간결하게 리뷰하고 결론을 빠르게 내라

## Task 정의 ({{task_filename}})
```markdown
{{task_content}}
```

다음 순서로 리뷰를 수행해라:

1. 위 Task의 완료 조건을 확인해라
2. 이 브랜치에서 변경된 코드를 git diff {{base_branch}} 으로 확인해라
3. 완료 조건을 하나씩 검증해라
4. 테스트가 있으면 실행해서 통과 여부를 확인해라
5. **문서 변경인 경우**: 문서에 기술된 파일명, 함수명, 흐름이 실제 코드와 일치하는지 대조해라.
   - 문서에 언급된 파일이 실제로 존재하는지 `ls`/`find`로 확인
   - 문서에 기술된 signal 이름, frontmatter 키, API 경로가 코드와 일치하는지 `grep`으로 확인
   - 불일치가 1건이라도 있으면 수정요청

리뷰 결과를 **반드시** 다음 형식으로 출력해라. 형식을 지키지 않으면 파싱 실패로 자동 수정요청 처리된다:

## 리뷰 결과
**Decision**: APPROVE 또는 REJECT (반드시 이 두 단어 중 하나를 정확히 사용)

### 완료 조건 체크
- [ ] 또는 [x] 각 완료 조건 항목

### 발견된 문제 (있을 경우)
- [파일명:줄번호] 구체적 문제 설명

### 총평
- 한줄 요약
